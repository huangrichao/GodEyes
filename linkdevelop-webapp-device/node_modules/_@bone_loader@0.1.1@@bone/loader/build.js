const fs = require("fs");
const path = require("path");
const config = require("./config.json");

fs.writeFileSync(
    path.join(__dirname, "loader.js"),
    "module.exports = window." + config["global-name"] + ";"
);